{
  "name": "vite-typescript-starter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test-serve-prod": "npm run build && npx http-server -S -C .cert/cert.pem -K .cert/key.pem -o -a localhost --cors dist/"
  },
  "dependencies": {
    "@johncision/applejacks": "^5.0.1",
    "@johncision/fluttershy": "^3.0.8"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  }
}
